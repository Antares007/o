%include "m.A"

function putchars
  push  rbp
  mov   rbp, rsp
  sub   rsp, 8
  
  mov   byte [rbp - 8], dil
  shr   rdi, 8
  mov   byte [rbp - 7], dil
  shr   rdi, 8
  mov   byte [rbp - 6], dil
  shr   rdi, 8
  mov   byte [rbp - 5], dil
  shr   rdi, 8
  mov   byte [rbp - 4], dil
  shr   rdi, 8
  mov   byte [rbp - 3], dil
  shr   rdi, 8
  mov   byte [rbp - 2], dil
  shr   rdi, 8
  mov   byte [rbp - 1], dil

  mov   edi, 0x1
  lea   rsi, [rbp - 8]
  mov   edx, 8
  mov   eax, 0x1
  syscall

  add   rsp, 8
  pop   rbp
return

tail:
