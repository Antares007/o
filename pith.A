%include "m.A"

import read
import write
import plus

  mov   edi, 0x0          ; fd
  lea   rsi, [rel buffer]       ; char*
  mov   edx, 3 ; length
  call  [rel read]

  mov   edi, 0x1          ; fd
  lea   rsi, [rel buffer] ; const char*
  mov   edx, 5            ; length
  call  [rel write]
  ret

buffer:
  db 'haha', 0xa
